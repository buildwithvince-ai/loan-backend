node_modules/

# Secrets
.env
.env.*

# OS cruft
.DS_Store
*.DS_Store

# Runtime logs (may contain applicant PII / FinScore payloads)
Logs/

# Sensitive captured artifacts in docs/ (HAR = headers/tokens, zip = applicant files, logs = PII)
docs/*.har
docs/*.zip
docs/*.log

# Local scratch / personal notes (not project docs)
BLOCKER.md
task.md
